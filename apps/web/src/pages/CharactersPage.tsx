import PagePlaceholder from '../components/PagePlaceholder';

export default function CharactersPage() {
  return <PagePlaceholder title="Characters" subtitle="Browse AI companions — coming in a later story." />;
}
